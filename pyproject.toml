[project]
name = "aarms-task-manager"
version = "1.0.0"
description = "AARMS Task Manager - Flask web application"
requires-python = ">=3.9"
dependencies = [
    "flask>=3.0.0",
    "werkzeug>=3.0.0",
]

[tool.vercel]
entrypoint = "task_app:app"
